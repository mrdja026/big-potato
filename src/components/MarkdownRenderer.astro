---
import { parseCustomBlocks } from "../utils/markdownParser";

interface Props {
  content: string;
}

const { content } = Astro.props;
const parsedContent = parseCustomBlocks(content);
---

<div set:html={parsedContent} />
